# docker compose up -d --build
# docker compose down

# Our entire services (containers)
services:

  # MySQL service: 
  mysql-service:
    image: mysql:8
    container_name: mysql-container
    environment:
      MYSQL_HOST: localhost
      MYSQL_DATABASE: vacation_app
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}

    volumes:
      - mysql-volume:/var/lib/mysql
      - ./Database:/docker-entrypoint-initdb.d

    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -uroot -p$$MYSQL_ROOT_PASSWORD"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

# ---------------------------------------------------------------------------------------------------

  # Our backend service:
  backend-service:
    depends_on:
      mysql-service:
        condition: service_healthy

    build:
      context: .
      dockerfile: ./Backend/Dockerfile
    
    image: vacation-backend-image:1.0
    container_name: backend-container

    environment:
      ENVIRONMENT: "production"
      PORT: 4000
      MYSQL_HOST: mysql-service
      MYSQL_USER: root
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      MYSQL_DATABASE: vacation_app
      HASH_SALT: ${HASH_SALT}
      JWT_SECRET: ${JWT_SECRET}
      CHAT_GPT_API_KEY: ${CHAT_GPT_API_KEY}
      MCP_SERVER_URL: "https://physiologically-curial-lakesha.ngrok-free.dev/sse"
      IMAGES_BASE_URL: "http://localhost:4000/api/vacations/images"

    ports:
      - 4000:4000

    volumes:
      - images-volume:/app/src/1-assets/images

    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "--eval", "require('http').get('http://localhost:4000/', response => process.exit(response.statusCode === 404 ? 0 : 1))"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

# ---------------------------------------------------------------------------------------------------

  # Our frontend website: 
  frontend-service:
    depends_on:
      backend-service:
        condition: service_healthy

    build:
      context: .
      dockerfile: ./Frontend/Dockerfile

    image: vacation-frontend-image:1.0
    container_name: frontend-container

    ports:
      - 80:5173

    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "--eval", "require('http').get('http://localhost:5173', response => process.exit(response.statusCode === 200 ? 0 : 1))"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

# ---------------------------------------------------------------------------------------------------

volumes:
  mysql-volume:
  images-volume:
